let initialState = {
    friends: [
        {id: 1, name: 'Dima'},
        {id: 2, name: 'Andrey'},
        {id: 3, name: 'Alina'}
    ]
}

const sideBarReducer = (state = initialState, action) => {

    return state
}

export default sideBarReducer